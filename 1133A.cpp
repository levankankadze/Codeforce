#include <iostream>
using namespace std;

void out(int h1, int h2, int m1, int m2){
    if(((h1 * 60 + m1+h2 * 60 + m2)/2/60)>=10 && ((h1 * 60 + m1+h2 * 60 + m2)/2%60)>=10)
        cout<<(h1 * 60 + m1+h2 * 60 + m2)/2/60<<":"<<(h1 * 60 + m1+h2 * 60 + m2)/2%60;
    if(((h1 * 60 + m1+h2 * 60 + m2)/2/60)<10 && ((h1 * 60 + m1+h2 * 60 + m2)/2%60)<10)
        cout<<0<<(h1 * 60 + m1+h2 * 60 + m2)/2/60<<":"<<0<<(h1 * 60 + m1+h2 * 60 + m2)/2%60;
    if(((h1 * 60 + m1+h2 * 60 + m2)/2/60)>=10 && ((h1 * 60 + m1+h2 * 60 + m2)/2%60)<10)
        cout<<(h1 * 60 + m1+h2 * 60 + m2)/2/60<<":"<<0<<(h1 * 60 + m1+h2 * 60 + m2)/2%60;
    if(((h1 * 60 + m1+h2 * 60 + m2)/2/60)<10 && ((h1 * 60 + m1+h2 * 60 + m2)/2%60)>=10)
        cout<<0<<(h1 * 60 + m1+h2 * 60 + m2)/2/60<<":"<<(h1 * 60 + m1+h2 * 60 + m2)/2%60;
}

int main(){
    int h1,m1,h2,m2;
    char x;
    cin>>h1>>x>>m1>>h2>>x>>m2;
    out(h1, h2, m1, m2);
    return 0;
}